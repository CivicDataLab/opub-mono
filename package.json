{
  "name": "turbo",
  "version": "0.0.0",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "lint": "turbo run lint",
    "format": "prettier --write \"**/*.{ts,tsx,md}\"",
    "test": "turbo run test --filter ui",
    "test:watch": "turbo run test:watch"
  },
  "devDependencies": {
    "classnames": "^2.3.2",
    "eslint-config-custom": "*",
    "prettier": "latest",
    "sass": "^1.58.0",
    "turbo": "^1.7.4"
  },
  "engines": {
    "node": ">=14.0.0"
  },
  "dependencies": {
    "@opub-icons/ui": "^0.9.2",
    "@opub-icons/workflow": "^0.9.3"
  },
  "packageManager": "yarn@1.22.17"
}
